#Desafios aula teste
#Crie um programa que leia dois numeros e mostre a soma entre eles.

n1 <- as.numeric(readline(prompt = "Digite o primeiro valor: "))
n2 <- as.numeric(readline(prompt = "Digite o segundo valor: "))

print(paste("A soma dos valores é igual a: ",n1+n2))
